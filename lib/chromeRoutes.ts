/** Routes that render their own navigation (e.g. the marketing landing page)
 * and opt out of the global DashboardNav + its reserved bottom-nav padding. */
export const CHROME_HIDDEN_ROUTES = ["/welcome"];
