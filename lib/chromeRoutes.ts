/** Routes that render their own navigation (the marketing landing page at /)
 * and opt out of the global DashboardNav + its reserved bottom-nav padding. */
export const CHROME_HIDDEN_ROUTES = ["/"];
